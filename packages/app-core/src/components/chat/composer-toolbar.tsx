/**
 * Composer-style toolbar shown under sent user bubbles. Wire references / attachments
 * when message payloads include them; until then this renders nothing so the import resolves.
 */
export function ReferenceComposerToolbar() {
  return null
}
