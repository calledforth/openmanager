{
  "name": "@openmanager/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "pnpm --filter @openmanager/protocol build && pnpm --filter @agentpack/runtime build && node --watch src/main.ts",
    "build": "pnpm --filter @openmanager/protocol build && pnpm --filter @agentpack/runtime build && tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "typecheck": "pnpm --filter @openmanager/protocol build && pnpm --filter @agentpack/runtime build && tsc --noEmit -p tsconfig.json",
    "lint": "eslint src tests vitest.config.ts",
    "test": "pnpm build && vitest run"
  },
  "dependencies": {
    "@agentpack/runtime": "workspace:*",
    "@openmanager/protocol": "workspace:*",
    "ws": "8.21.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "@types/ws": "8.18.1",
    "eslint": "^9.39.4",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
